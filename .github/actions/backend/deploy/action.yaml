name: Backend deploy
description: 'Deploy backend cloudflare worker'
runs:
  using: composite
  steps:
    - uses: pnpm/action-setup@v3

    - name: Install PNPM
      shell: bash
      run: cd backend && pnpm install

    - name: Deploy worker
      shell: bash
      run: cd backend && pnpm run deploy
